{
  "//": "Hand-edited corrections applied to entity_models.json + entity_geometry.json. Loaded by EntityModelLoader per entity; honoured keys are `geometry_ref`, `texture_ref`, `hidden_bones`, `overlays`. `hidden_bones` strips bones the geometry packs into a shared tree but the entity should not show (illager hat, equine saddle, armor stand hat).",
  "entities": {
    "//minecraft:cow_cold": "ColdCowModel registers a separate LayerDefinition (ModelLayers.COLD_COW) with a 3-cube body (base + inflated 0.5 fur + udder strip) and a head whose horns are child bones with PartPose.offsetAndRotation(±, -2.5, -3.5 or -5, PI/2, 0, 0). Bytecode tooling only scans top-level renderer registrations; variant ModelLayers aren't surfaced. Hand-edit redirects minecraft:cow_cold at geometry.cold_cow.",
    "minecraft:cow_cold": {
      "geometry_ref": "geometry.cold_cow"
    },
    "//minecraft:cow_warm": "WarmCowModel REPLACES head with an inline 6-cube list (base head + muzzle + 4 horn cubes including 2 mirrored at +X positions). Same tooling miss as cow_cold; hand-edit redirects at geometry.warm_cow.",
    "minecraft:cow_warm": {
      "geometry_ref": "geometry.warm_cow"
    },
    "//minecraft:chicken_cold": "ColdChickenModel REPLACES body (base + 0x3x5 plane-cube tail at uv (38, 9)) and head (base + 6x3x4 crown at uv (44, 0)). Replacing head wipes beak + red_thing children since addOrReplaceChild's new CubeListBuilder doesn't re-register them. Variant ModelLayers.COLD_CHICKEN not surfaced by tooling; hand-edit redirects at geometry.cold_chicken. The warm chicken variant (chicken_variant/warm.json) has no model override and falls through to AdultChickenModel - it's a family-fit canvas issue, not a geometry issue, so chicken_warm stays on geometry.adultchicken.",
    "minecraft:chicken_cold": {
      "geometry_ref": "geometry.cold_chicken"
    },
    "//minecraft:pig_cold": "ColdPigModel REPLACES body with base 10x16x8 + inflated 0.5 fur layer (uv (28, 32)). Head and legs inherited. Variant ModelLayers.COLD_PIG not surfaced by tooling; hand-edit redirects at geometry.cold_pig. The warm pig variant (pig_variant/warm.json) has no model override and falls through to PigModel - same family-fit canvas issue as chicken_warm.",
    "minecraft:pig_cold": {
      "geometry_ref": "geometry.cold_pig"
    },
    "//minecraft:stray": "Vanilla StrayRenderer adds a SkeletonClothingLayer (ModelLayers.STRAY_OUTER_LAYER + stray_overlay.png). STRAY_OUTER_LAYER is HumanoidModel.createMesh(CubeDeformation(0.25F), 0.0F) - the standard skeleton mesh with every cube inflated by 0.25 pixels. Without this overlay the skeleton-only silhouette is missing the frost-cloak content that protrudes laterally.",
    "minecraft:stray": {
      "overlays": [
        {
          "geometry_ref": "geometry.humanoid_outer",
          "texture_ref": "skeleton/stray_overlay",
          "inflate": 0.25
        }
      ]
    },
    "//minecraft:bogged": "Vanilla BoggedRenderer adds a SkeletonClothingLayer (ModelLayers.BOGGED_OUTER_LAYER + bogged_overlay.png). BOGGED_OUTER_LAYER is HumanoidModel.createMesh(CubeDeformation(0.2F), 0.0F) - skeleton mesh with inflate 0.2. Bogged's base geometry already includes the mushroom bones; this overlay supplies the inflated humanoid silhouette that vanilla renders on top.",
    "minecraft:bogged": {
      "overlays": [
        {
          "geometry_ref": "geometry.humanoid_outer",
          "texture_ref": "skeleton/bogged_overlay",
          "inflate": 0.2
        }
      ]
    },
    "//minecraft:cat": "Vanilla Java's AdultFelineModel.setupAnim sets `tail2.xRot = 1.7278761f + amplitude * cos(walkPos) * walkSpeed` on three sibling state branches (sprinting/walking/crouching). The static rest pose (walkSpeed=0) collapses to 1.7278761 rad = ~99 deg - the bend at the tail1/tail2 joint that gives a cat its hooked-tail silhouette. tail1's rotation lives in createBodyLayer and is auto-captured by the bind-pose scraper; tail2's lives only inside setupAnim so the scraper misses it. We override BOTH bones with bone-level rotation (not bind-pose) so the chain propagates from tail1 to tail2 - bind-pose only rotates the bone's own cubes, leaving children at their authored joint, which would detach tail2 from tail1's swept-up tip. The override on tail1 also clears the auto-generated bind_pose_rotation (see applyBoneOverrides) to avoid stacking the same 51.566 deg twice. tail1 pivot [0, 9, 8] and tail2 pivot [0, 9, 16] are the geometry's authored joints so each segment swings from its base. Texture: vanilla's zero-state cat variant resolves to the BLACK (tuxedo) entry from data/minecraft/cat_variant/black.json - all variants except all_black (which is structure-gated to witch huts via cats_spawn_as_black) are priority=0 unconditional, and the harness picks alphabetically first: black. The tuxedo pattern has a black body + white chest / paws + gray collar, dominant rgb (28, 24, 39) and (234, 234, 234). Our tooling default cat_all_black is the uniform-black stray variant; cat_black is the tamed-tuxedo variant the harness renders.",
    "minecraft:cat": {
      "texture_ref": "cat/cat_black"
    },
    "//minecraft:zombified_piglin": "AdultZombifiedPiglinModel.createBodyLayer just returns AdultPiglinModel.createBodyLayer() - the two layers are bytecode-identical. Bytecode tooling emits them as separate entries (geometry.adultpiglin + geometry.adultzombifiedpiglin) with the same bones; redirect to the hand-edited geometry.adultpiglin so the right_sleeve / right_pants pivot fixes and the dropped hat bone apply uniformly across the piglin family.",
    "minecraft:zombified_piglin": {
      "geometry_ref": "geometry.adultpiglin"
    },
    "//llama-family-chest": "LlamaModel.createBodyLayer unconditionally adds right_chest / left_chest bones; LlamaModel.setupAnim toggles their visibility via state.hasChest, which is equipment-driven and false at zero state. Bytecode tooling captures the bones from createBodyLayer but doesn't see the setupAnim visibility toggle, so the chests render by default. Hide them to match a freshly-spawned (no chest equipped) llama. Same applies to trader_llama: the chest is still equipment - the trader-signature decoration is the carpet rendered by LlamaDecorLayer on top of the inflated decor model, NOT the chest.",
    "minecraft:llama": {
      "hidden_bones": [
        "right_chest",
        "left_chest"
      ]
    },
    "//minecraft:tropical_fish": "TropicalFishRenderer.getModelTint returns state.baseColor (DyeColor.WHITE.textureDiffuseColor = 0xFFF9FFFE at zero state, just shy of pure white). TropicalFishPatternLayer submits a second textured pass with state.patternColor (also 0xFFF9FFFE at zero state DEFAULT_VARIANT = (KOB, WHITE, WHITE)). Both tints are multiplicative against the sampled texel - tiny shift visible across the silhouette. The pattern model is a separate LayerDefinition baked with FISH_PATTERN_DEFORMATION = CubeDeformation(0.008F) (LayerDefinitions.java line 164) - that's an inflate on every pattern cube, not just a depth-test offset, so the pattern silhouette is geometrically 0.008 wider than the base body per side. Vanilla harness includes this in its bounds-walker, padding the canvas by 1 pixel on each axis; matching this here (inflate 0.008 vs the previous generic 0.001) lines up canvas dimensions AND the per-cube extents the rasterizer walks.",
    "minecraft:tropical_fish": {
      "base_tint": "0xFFF9FFFE",
      "overlays": [
        {
          "geometry_ref": "geometry.tropicalfishsmall",
          "texture_ref": "fish/tropical_a_pattern_1",
          "inflate": 0.008,
          "tint_color": "0xFFF9FFFE"
        }
      ]
    },
    "//minecraft:trader_llama": "Trader llamas additionally have LlamaDecorLayer rendering EquipmentAssets.TRADER_LLAMA on top of an inflated copy of the llama mesh (ModelLayers.LLAMA_DECOR = LlamaModel.createBodyLayer(CubeDeformation(0.5))). The TRADER_LLAMA equipment asset points at textures/entity/equipment/llama_body/trader_llama.png which carries the carpet pattern; chest UV is transparent so the overlay's still-present chest bones contribute nothing visible. The overlay carries inflate 0.5 to wrap the base llama body, matching vanilla's CubeDeformation(0.5F) on the decor layer.",
    "minecraft:trader_llama": {
      "hidden_bones": [
        "right_chest",
        "left_chest"
      ],
      "overlays": [
        {
          "geometry_ref": "geometry.llama",
          "texture_ref": "equipment/llama_body/trader_llama",
          "inflate": 0.5,
          "skip_bounds": true
        }
      ]
    }
  }
}
